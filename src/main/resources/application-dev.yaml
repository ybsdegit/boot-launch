server:
  port: 8080

spring:
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://112.126.101.188:3306/testdb1?serverTimezone=UTC&useSSL=false
    username: root
    password: mima

#mybatis:
#  mapper-locations: classpath:generator/*.xml

logging:
  level:
    com.zimug.bootlaunch: debug

# 驼峰命名自动映射
mybatis:
  configuration:
    mapUnderscoreToCamelCase: true
